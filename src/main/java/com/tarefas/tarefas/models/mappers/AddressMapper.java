package com.tarefas.tarefas.models.mappers;

public class AddressMapper {
//    public AddressDTO addressToDTO(Address address){
//        return new AddressDTO(address.getCep(), address.getLogradouro(), address.getComplemento(), address.getBairro(), address.getUf());
//    }
//
//    public Address DTOToEntity(AddressDTO dto){
//        return new Address(dto.getCep(), dto.getLogradouro(), dto.getComplemento(), dto.getBairro(), dto.getUf());
//    }
}
